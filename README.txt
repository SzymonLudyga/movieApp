Simple movie API that allows to add movie from external OMDB API with detailed movie information to mongoDB.
It also allows to DELETE movies, POST comments, GET filtered comments and DELETE comments.
Express error handling is added to the app as well as ESlint and dotenv features.